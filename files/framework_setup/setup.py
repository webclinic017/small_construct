from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='virtual environment for small construction project to build out scalable model',
    author='gdp',
    license='',
)
